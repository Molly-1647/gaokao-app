# ============================================================
# Render 平台配置 - 原生Python运行时（无需Docker）
# 部署更简单、内存占用更低、成功率更高
# ============================================================

services:
  - type: web
    name: gaokao-app
    runtime: python
    plan: free
    region: singapore
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:create_app('production') --workers 1 --bind 0.0.0.0:$PORT --timeout 60
    envVars:
      - key: FLASK_ENV
        value: production
      - key: FLASK_CONFIG
        value: production
      - key: FORCE_TFIDF
        value: "1"
      - key: TZ
        value: Asia/Shanghai
      - key: SECRET_KEY
        generateValue: true
